#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Printf
 *
 * Return: 0 (ok)
 */

int main(void)
{
	int (n);

	for (n = 0; n <= 9; n++)
		printf("%d", n);
	printf("\n");

	return (0);
}
